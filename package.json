{
  "name": "my-portfolio",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://yeshwanthvarma.github.io/my-portfolio",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build:legacy",
    "deploy": "gh-pages -d build",
    "build:legacy": "NODE_OPTIONS=--openssl-legacy-provider react-scripts build"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "web-vitals": "^4.2.2"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
